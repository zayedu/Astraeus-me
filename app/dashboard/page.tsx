import { MainLayout } from "@/components/main-layout"

export default function DashboardPage() {
  // This page renders the MainLayout for a new PAR creation flow.
  // The `parId` is omitted, so it starts in default chat mode.
  return <MainLayout />
}
